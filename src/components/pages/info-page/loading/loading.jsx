import React from 'react';

import Info from '../info/info';
import {InfoText} from '../../../../const';

const Loading = () => {
  return (
    <Info infoText={InfoText.LOADING} />
  );
};

export default Loading;
